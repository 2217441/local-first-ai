# local-first-ai

Landing page for local-first AI infrastructure consulting.

## Ship checklist

- [ ] Record `demo.mp4` (75s, 1080p): messy folder → agent queries machine map → **gate rejection moment** → clean output. Drop into repo root — the `<video>` tag picks it up automatically.
- [ ] Optional: `gate.gif` screenshots for the proof strip (`[screenshot: …]` placeholders in index.html).
- [ ] Replace contact constants in `index.html`: `wa.me/6XXXXXXXXXX` and `YOUR@EMAIL.COM`.
- [ ] GitHub → Settings → Pages → Source: **Deploy from branch → main → root**.
- [ ] PDF version = browser Ctrl+P on the live page (print stylesheet included).
